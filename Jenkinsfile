pipeline {
    agent any

    stages {
        stage('Hello') {
             steps {
                /*
                script {
                    def now = new Date()
                    println now.format("yyyyMMddHHmm", TimeZone.getTimeZone("Asia/Seoul"))
                }
                */
                echo "Hello, World!"
            }
        }
    }
}
